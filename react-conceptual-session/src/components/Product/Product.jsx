
export default function Product({info}) {
    // const {info} = props;
    console.log(info);
    return (
        <div>
            <h2>I am from Product of Products Child...</h2>
        </div>
    );
}